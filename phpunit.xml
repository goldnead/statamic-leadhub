<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         processIsolation="false"
         stopOnFailure="false"
         cacheDirectory=".phpunit.cache"
>
    <testsuites>
        <testsuite name="Unit">
            <directory suffix="Test.php">./tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory suffix="Test.php">./tests/Feature</directory>
        </testsuite>
        <!--
            The migration path itself, against tables that already hold rows.
            Every other suite meets a database RefreshDatabase has migrated to
            head; these install an earlier release, put data in and migrate
            forward. Present in both configurations on purpose — the failure
            that prompted them aborts with a unique violation on SQLite and with
            ERROR 1062 on MySQL, and neither engine rolls the half state back.
        -->
        <testsuite name="Migrations">
            <directory suffix="Test.php">./tests/Migrations</directory>
        </testsuite>
        <!--
            Live integration with the optional goldnead/statamic-webhook-manager
            peer addon. These tests skip themselves when the addon is not
            installed, so they are a no-op in the default suite; install the peer
            (scripts/test-webhook-manager.sh) to exercise them for real.
        -->
        <testsuite name="Integration">
            <directory suffix="Test.php">./tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>./src</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
    </php>
</phpunit>
